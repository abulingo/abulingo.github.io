{
  "name": "abulingo",
  "private": true,
  "scripts": {
    "build:css": "tailwindcss -c tailwind.config.js -i src/app.css -o app.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "3.4.17"
  }
}
